/* ============================================================
   Real Local — 런타임 설정

   빌드 단계가 없는 정적 사이트라 .env를 읽을 주체가 없다.
   서버도 번들러도 없으므로 설정은 이 파일에 직접 적는다.

   ------------------------------------------------------------
   ⚠️ 이 파일은 브라우저에 그대로 전송된다. 레포도 public이므로
      여기 적는 키는 누구나 볼 수 있다고 전제해야 한다.

      Google Maps JavaScript API 키는 원래 공개되는 것이 정상이며,
      보안은 키를 숨기는 것이 아니라 '제한'으로 건다. 키를 넣기
      전에 Google Cloud Console에서 반드시 아래 두 가지를 설정할 것:

      1) 애플리케이션 제한 → HTTP 리퍼러
           https://stella849.github.io  (뒤에 슬래시+별표)
           http://localhost             (포트와 경로에 별표)

      2) API 제한 → Maps JavaScript API 만 선택

      리퍼러 패턴을 여기 그대로 적지 않는 이유는 별표와 슬래시 조합이
      이 주석을 닫아버리기 때문이다. Console 도움말의 예시를 따를 것.

      이 두 가지가 걸려 있지 않은 키는 절대 여기 넣지 말 것.
      제한 없는 키가 유출되면 타인이 우리 결제 계정으로 호출한다.
   ------------------------------------------------------------ */

window.RL_CONFIG = {
  /* 비워 두면 지도는 Leaflet + CARTO(키 불필요)로 뜬다.
     제한을 건 키를 넣으면 Google Maps로 자동 전환된다.
     키가 잘못됐거나 스크립트 로드가 실패하면 Leaflet으로 되돌아가므로
     지도가 빈 채로 남는 일은 없다. */
  googleMapsApiKey: 'AIzaSyDtQJxJDWVP8FDZZxEiFz7_aT83VrEl7V0',

  /* 선택 사항. Cloud Console > 지도 스타일에서 만드는 Map ID.
     넣으면 번호 핀이 앱과 똑같은 모양(AdvancedMarkerElement)으로 뜨고,
     비워 두면 구글 기본 마커에 번호 라벨만 얹힌다. 없어도 동작한다. */
  googleMapsMapId: '',
};
